<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ALTimer" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/lightyellow</item>
        <item name="colorPrimaryVariant">@color/backyellow</item>
        <item name="colorOnPrimary">@color/backyellow</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Theme.ALTimer.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@android:color/black</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/lightyellow</item>
        <item name="colorPrimaryVariant">@color/backyellow</item>
        <item name="colorOnPrimary">@color/backyellow</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:windowBackground">@color/backyellow</item>
        <item name="android:statusBarColor">@color/lightyellow</item>
    </style>

    <style name="Theme.ALTimer.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="DeleteDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/deletedialogbackground</item>
    </style>

</resources>